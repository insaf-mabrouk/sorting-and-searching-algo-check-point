FUNCTION binary_search(arr: array of integer, n:integer)
var
  i, j, x: integer
begin
 for (i=1; i<arr.length;i++){
    x=arr[i]
     j=i-1;
     while (j>=0 && arr[j]>x)
           arr[j+1]=arr[j]
           j--;
    end while
       Arr[j]=x
  end for
 end      
  